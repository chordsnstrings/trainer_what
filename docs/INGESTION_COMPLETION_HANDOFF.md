# Knowledge import completion — 26 September 2026

Files: `apps/api/src/ingestion.ts`, `apps/web/components/ingestion-review.tsx`, `scripts/{office_safety,extract-docx,extract-xlsx,extract-csv}.py`, `tests/ingestion-completion.test.ts`. No new migration or npm dependency.

- Existing `ingestionRoutes(app,db,trainer)` registration remains. Formats: UTF-8 text/Markdown, parsed CSV/TSV, visible literal XLSX cells, DOCX, validated structured program JSON, selectable/scanned PDF and JPEG/PNG/WebP images. Structured program input matches existing domain program schema (single program or bounded programs array); it becomes teaching material after review, never silently assigns workouts.
- Local extraction uses installed Python standard library, Poppler and Tesseract English. Add `tesseract-ocr` to Dockerfile and both CI apt-install lines beside python3/poppler-utils. No network model calls or extra npm packages. Production file imports still require existing FILE_IMPORTS_APPROVED review control.
- Bounds: 5 MB input; 60,000 extracted characters; at most 30 PDF pages including six scanned pages; 24 MP nonanimated image; two local extractions at once; child timeout20s, output700KB, single OCR thread; archive512 entries, 8MB/member, 40MB expanded, ratio100. Reject XML entities/DTD, duplicate/traversal/symlink entries, external relationships/data links, macros/embedded files and spreadsheet formulas. CSV cells/rows and XLSX shared strings are bounded. Hidden sheet/row contents are not imported; spreadsheet date serials remain numbers, disclosed in review.
- POST `/brain/documents` now creates private `source_import` records with processing lease, lineage/hash and `allowedUses:[]`; it returns needs_review on extraction success. Original temp file and derived image/page files are removed in finally. Failure persists only metadata/error and supports re-upload of the same bytes with retryOf. A stale processing claim can retry after five minutes. Existing upload deduplication/lineage response remains compatible.
- GET `/brain/imports` lists private imports; POST `/:id/redact` removes flagged identifiers; POST `/:id/review` requires revision, reviewed text/title, rights and privacyReviewed acknowledgments. It rejects remaining detected personal identifiers and creates an eligible ready source. Reviewed text has ≤40 chunks, each≤2000 characters, offset/hash lineage. Raw extracted text and identifier matches are removed from the import after approval. DELETE `/:id` with revision discards only an unapproved import and clears extracted content. Detectors are assistance, not a claim of comprehensive de-identification; full manual review is required.
- Mount `KnowledgeImportReview({onApproved:refresh})` in Knowledge tab instead of old document-upload details. Public accepted formats and explicit processing/review/failure/retry/discard states are included. Textareas allow extraction correction and redaction before model use.
- In app.ts manual `/brain/sources`, call `privacyMatches(b.text)` and reject detected identifiers with a redaction/review message; merge `...buildSourceEvidence(b.text)` into source data to provide the same chunks/hash. Manual source form already attests removal of unnecessary personal data.
- Before provider compilation, call `compilationMaterial(material)` instead of directly mapping full record data. It enforces source ready/interview answered and both model_prompt+trainer_specific_learning rights, max20 sources/max120k total characters. It returns minimal `{id,data}` without private metadata or duplicated chunks. source_import is never an eligible Brain source. Existing retrieval runtime only consumes published coach rules and teaching cases.
- Privacy owner is notified to include source_import in trainer/workspace cleanup. Subscriber/finance RLS already excludes this private kind. Raw source bytes are never stored in the database.

Verification: focused import suite passed8/8 including actual image and scanned-PDF OCR, strict spreadsheet/XML/archive paths, private review/PII-redaction, retry/discard, temp-file cleanup and compilation limits/status/rights/minimal payload. The three existing document import/PDF/DOCX extraction regression tests also passed (3/3). No cloud browser, screenshots, deployment or provider traffic.
