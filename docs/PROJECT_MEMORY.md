# Project memory

Updated: 25 September 2026. This is durable project context for future build sessions. It records decisions, not credentials or a claim about account capabilities.

## Owner decisions

| Topic | Current decision |
| --- | --- |
| Project | `chordsnstrings/trainer_what`; Trainer Brain Platform |
| Current request | Get the application running in a wholly new DigitalOcean project named GymMembership with automatic deployment of tested Git changes. Then continue the complete nutrition scope, including required meal-photo logging/barcode scanning under 043 and expanded 044 evidence. Routine case-taught nutrition remains automatic, with human handling of exceptions. |
| Astra | Available for difficult design, implementation and review; use tokens wisely; do not use it for browsing |
| Execution | Use bounded tasks, targeted context, deterministic tooling and useful verification. On 25 September at 13:44 Asia/Dubai, the owner explicitly requested multiple agents for speed; split nonoverlapping implementation/review work and keep cloud writes with the coordinating agent. Avoid repeated planning/research and unnecessary confirmation. |
| Collections | Existing operator Stripe account for subscriber subscriptions, refunds and disputes |
| Trainer payouts | Lean Technologies initiates monthly payouts from the company's bank account to eligible verified personal/business UAE IBANs, after reconciliation |
| Payment history | The earlier Stripe Connect payout direction is superseded by Stripe collection + Lean payout |
| Finance | One immutable ledger, marginal 25%/20%/15%/10% commission bands, transparent AI/voice charges and gross-to-net statements |
| Product | Trainer Brain Compiler + Client Twin + governed first-person Coach Runtime; trainer identity and control are central |
| Nutrition product | Two subscriber tiers: workout only and higher-priced workout + nutrition. Coach supplies diet and approximate calorie guidance; AI delivers daily meals, recipes, portions, cooking options and consolidated weekly groceries. No nutrition-only subscriber tier in current scope. Nutrition core is implemented and CI-verified; consult BUILD_STATUS.md for the exact scope and production gates. |
| Meal photos and barcodes | Required features of workout + nutrition, confirmed by the owner on 25 September 2026. Photo → editable food/portion/calorie estimate → subscriber confirmation → diary. Barcode → sourced product/serving details → subscriber confirmation → diary. No routine coach approval or third subscriber tier. Feature scope is locked; neither integration is implemented or provider-verified yet. |
| Nutrition teaching and autonomy | Case-based onboarding captures what the coach recommends, why, alternatives, conditions and limits. Routine in-scope plans/changes should run automatically; human attention goes to exceptions, not every output. Implemented mechanism: private versioned cases/rules with independent held-out evaluation and explicit action policies; no per-coach model-weight training is claimed. |
| Market/design | UAE, AED, English first with Arabic-ready layout; restrained Swedish-minimal design |
| Infrastructure | New DigitalOcean project and resources authorized at 12:35 on 25 September 2026. At 13:24 the owner required the exact project name GymMembership and deferred dedicated VPC/cloud firewall work for a single-server start. Never change an existing project/resource; use only recorded create-response IDs for writes. Default networking may be used without modification. Initial selection: blr1, s-2vcpu-4gb, USD 24/month compute cap, rechecked before creation. Infrastructure authority does not establish customer-data residency or provider/legal qualification. |
| Credentials | Operator expects to supply API access during implementation; availability and permissions must be verified without retaining values here |
| Git deployment | Required since 12:47 on 25 September: automatically deploy successful checked `main` commits only to the new owned environment. GitHub setup plus a five-minute public-repository host updater are implemented locally; CI and live verification remain pending. DO_PROVISION_TOKEN Actions secret and live verification are pending. |
| Access preference | The owner explicitly requires direct DigitalOcean API use and says NEVER USE CLOUD BROWSER. Do not use cloud browser or request browser sign-in again. Direct API token validity remains unverified because the account request returned Site Unavailable HTML. The GitHub connector cannot configure Actions secrets; the prepared GitHub setup route needs one-time operator entry. |

## Current state

- Core nutrition code checkpoint: `552d3843d0faea09d28633a1ac84816701994d36`; final client fixes: `2bf35fda06c3fc58bcfd8f23837095057e911972` on `main`. Next.js web, Fastify API, PostgreSQL/PGlite, worker and ten migrations. Work 035–042 core is implemented; this does not complete the original 34-package product.
- Verified final code `2bf35fd` in [CI run 36094669632](https://github.com/chordsnstrings/trainer_what/actions/runs/36094669632): TypeScript, 51 tests each with PGlite and PostgreSQL 17.6/non-owner runtime role, production web/container build, API readiness and 32 browser routes. The original workout/onboarding/Twin journeys and nutrition meals, groceries, pantry, conditional setup and offline diary replay passed. Nutrition desktop/mobile captures were visually reviewed. The final empty-queue reconnection check also passed; artifact `10846757453` retains the final report and screenshots. Exact evidence: `VERIFICATION_2026-09-25_NUTRITION.md`.
- No secret was copied from chat into code; no live Stripe/Lean/model/email call, charge, refund, payout, purchase or DigitalOcean deployment occurred. Stripe credentials supplied in chat are not configured or verified. DigitalOcean app account/region/size reads succeeded at 13:11 Asia/Dubai; a later direct account request with the supplied token returned HTTP 200 HTML (Site Unavailable), so token validity remains unverified. The app lacks project creation/assignment and cloud-init parameters. The prepared GitHub setup workflow supplies those operations through the official API, but no real resources have been created.
- Stripe → company bank → Lean → verified trainer IBAN remains the selected flow. Account-specific Lean transport, personal/business recipient eligibility, bank authorization and finality remain unverified and execution-gated. Usage accounting now reserves before sending; invalid outputs retain usage, unknown cost blocks close, finance can reconcile evidence, and a configurable daily request cap defaults to 100 per workspace.
- Existing platform scope: all 16 workout-only onboarding steps have a persisted registry with computed readiness; identity autosave uses optimistic concurrency, preview changes invalidate review, publish gates run on the server. Client Twin snapshots retain source/freshness/coverage, deduplicate imported observations for baselines, remain scoped/versioned, and keep wearable-derived data out of model prompts. Dedicated public how-it-works/demo/pricing/FAQ pages are implemented.
- Full product remains unfinished. `BUILD_STATUS.md` distinguishes engineering gaps from access/approval blockers: acquisition attribution, brand/exercise media, broader ingestion/retrieval/Twin domains, automatic provider reconciliation, legal/provider/backup erasure operations, WHOOP/Zepp, domains, voice, native companion, campaigns/experiments/full admin views and infra Governor remain open. No group of the 75-screen source contract is declared complete from route coverage alone.
- Active engineering work: publish and CI-verify the GymMembership setup workflow and host deployment controller under work 005. Parallel provisioning/host reviews are complete and all 36 local deployment tests passed; only the coordinating agent may create cloud resources. The code is in the working tree, with publishing, CI and live evidence pending. Next nutrition slice remains 043 meal-photo capture/confirmation and barcode lookup/confirmation with private media, sourced food facts, diary/Twin integration, costs and manual fallbacks. Follow section 7 of NUTRITION_INTEGRATION_PLAN.md; neither feature is implemented or provider-qualified yet.
- Next infrastructure step: publish and CI-verify the setup code, then obtain a valid direct API response before creating only the new GymMembership resources. The prepared GitHub setup route needs DO_PROVISION_TOKEN in repository Actions secrets. Verify the new project, HTTPS and a subsequent checked Git update after setup. Cloud-browser use is prohibited. For nutrition activation, configure a separate nonproduction model with explicit prices, load real coach cases/recipes, pass held-out evaluations and sample-week qualification, and exercise worker delivery. Operator inputs for other providers, domain identity and legal/financial review remain open. Verify Stripe paired-tier and bank contracts before enabling live commerce. Production is not ready.

## DigitalOcean implementation checkpoint — 25 September 2026

- Exact new project name: GymMembership. New project, SSH key and server are authorized; all pre-existing resources remain outside write scope. Dedicated VPC and cloud firewall are deferred. Use default networking without changing it. Keep PostgreSQL/API/worker unexposed on the host and web on loopback behind HTTPS.
- Selected configuration in infra/digitalocean/launch.json: blr1, Ubuntu 24.04, s-2vcpu-4gb, USD 24/month compute cap. The earlier authenticated API quote established availability and price; the provisioner rechecks both before creation. No purchase or real resource ID exists.
- Local implementation: .github/workflows/gymmembership.yml and infra/digitalocean/{common,provision,host,dispatch}.py. Setup waits for exact-current-main Application checks, checkpoints creates on a dedicated Git branch, creates only new resources and verifies project assignment. The new host generates its own runtime secrets and polls the public repository about every five minutes for a successful checked main commit. No management token is placed on the server.
- Reviewed release behavior: validate Compose exposure, build a pinned commit, recheck CI, preserve the existing database volume, take a local SQL dump on updates, migrate with the administrator, apply the non-owner runtime role, verify HTTPS/readiness and record the SHA. Failed application health triggers an attempt to restore the previous application; database migrations are not reversed. Off-host backup and restore evidence remain pending.
- Current boundary: both code reviews are complete and all 36 local deployment unit tests passed. No new CI or actual cloud-init/HTTPS/Git-update proof exists yet. DO_PROVISION_TOKEN is not configured in repository Actions secrets. The GitHub connector lacks a secret-setting operation; that prepared route needs one-time operator entry. Adding the secret alone does not run setup.
- Access result: app reads work. The final direct account API call used the supplied token and returned HTTP 200, Content-Type text/html and a 195-byte Site Unavailable page instead of account JSON. Token validity remains unverified; no create calls were made. Earlier direct calls timed out at proxy CONNECT. Work network access was always enabled and the owner is the workspace administrator; do not repeat installation/toggle/admin requests. The owner explicitly requires direct DigitalOcean API access and prohibits cloud-browser use.
- Runtime flags for legal approval, imports, nutrition, bundle changes, commerce and payouts remain false. Infrastructure authorization does not approve live provider transactions, real health-data placement or required photo/barcode functionality. See DIGITALOCEAN_DEPLOYMENT.md for setup and evidence requirements.

## Nutrition implementation checkpoint — 25 September 2026

- Completed core work 035–042 and engineering release checks for 044: typed nutrition contracts, normalized immutable food/recipe/ingredient versions, case teaching, source imports, policy compilation/confirmation, independent held-out checks and preview-based activation, automatic plans/swaps/derived groceries, explicit exceptions, subscriber consent/profile/diary/check-ins, nutrition Twin, export/erasure and weekly jobs. Nutrition uses separate record kinds/releases so the training release and existing supervision remain unchanged.
- Conditional onboarding adds six steps after nutrition is enabled. Workout-only publishing stays independent; combined-offer activation requires nutrition readiness. Paired tiers enforce a higher combined price. Signed Stripe price mapping controls entitlements; billing-portal confirmation is gated by BUNDLE_CHANGES_APPROVED and real-account verification.
- Migration 010_nutrition and the core journeys passed PGlite/PostgreSQL, production web/container and browser verification. The final browser run additionally verified reconnection with an empty diary queue. Automatic scheduling uses durable first/next-week jobs; daily meals and groceries derive from the same stored plan revision.
- Model adapters use the configured provider and durable existing usage accounting. Evaluation tests use synthetic fetch fixtures, tagged as fixture verification; no real model/food provider was called. No live charge/refund/payout, infrastructure purchase or production deployment occurred. Nutrition demo data is explicitly synthetic and cannot qualify production.
- Production needs NUTRITION_ENABLED, qualified scope review (NUTRITION_SCOPE_APPROVED), actual coach/food material and successful configured-model evaluation; worker scheduling needs PostgreSQL. Existing provider, infrastructure and legal gates remain. Meal-photo and food-barcode integrations (043) are now required scope and not implemented. No provider/account/rights/storage contract has been selected; existing meal/recipe/grocery delivery uses versioned coach food facts. The earlier 044 pass covers that core only; the expanded photo/barcode journeys require fresh evidence.

## Handoff format

At the end of an implementation slice, replace the current-state bullets with a short factual checkpoint: active/completed issue IDs; code commit; migration/deployment state; checks actually run and evidence paths; unresolved blocker with owner; next executable action. Preserve the owner decisions until explicitly changed. Do not mark a requirement complete because its plan or UI exists.
